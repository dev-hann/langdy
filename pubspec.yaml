name: langdy
description: Langdy Clone Coding.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State manger
  provider:

  # Utils
  equatable:
  intl:
  dartz:

  # View
  carousel_slider:
  auto_size_text:
  cupertino_icons:
  sliding_up_panel:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
